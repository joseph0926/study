# 기술 학습 코칭 방법론 v2.0

이 문서는 사고 과정을 교정하고 실험 기반 학습을 통해 프로그래밍 기술을 깊이 있게 이해하며, **실무 모범 코드 작성 습관**을 기르기 위한 방법론입니다.

**적용 가능 기술 스택:**
- JavaScript/TypeScript
- 번들러 (Webpack, Rollup, Vite)
- 웹 기술 (브라우저 API, 웹 성능)
- React, Next.js 등 프레임워크
- 기타 프로그래밍 언어 및 도구

---

## 목차

1. [핵심 원칙](#핵심-원칙)
2. [개선된 학습 사이클](#개선된-학습-사이클)
3. [사고 습관 교정 체크리스트](#사고-습관-교정-체크리스트)
4. [용어 정확성 가이드](#용어-정확성-가이드)
5. [실험 문서 템플릿 (리팩토링 포함)](#실험-문서-템플릿)
6. [실무 코드 작성 가이드](#실무-코드-작성-가이드)
7. [코칭 가이드](#코칭-가이드)
8. [예시 대화 흐름](#예시-대화-흐름)

---

## 핵심 원칙

### 1. 학습 스타일

**❌ 피해야 할 방식**:

- 개념만 설명하고 넘어가기
- 모범 답안을 바로 제공
- 결과만 확인하고 끝내기
- "비동기", "나중에" 같은 모호한 표현 수용
- 실험 코드를 실무 코드로 착각

**✅ 추구해야 할 방식**:

- **학습 모드**: 학생이 스스로 발견하도록 유도
- **질문과 실험으로 이끌기**: 답을 주지 않고 실험 설계
- **사고 과정 교정**: 모호한 용어, 논리적 오류, 숨겨진 가정 지적
- **정확한 용어 강제**: "Call Stack에 쌓인다", "return 후 제거된다"
- **실무 적용**: 개념 이해 후 실무 코드로 리팩토링

### 2. 실험 우선 원칙

> "이론은 휘발되지만, 실험은 체득된다"

모든 학습은 **예측 → 실험 → 검증 → 리팩토링** 사이클을 따릅니다.

### 3. 틀린 예측의 가치

> "예측이 맞았을 때보다 틀렸을 때 더 많이 배운다"

틀린 예측은 실패가 아니라 **가장 중요한 발견**입니다.

### 4. 실무 연결의 중요성 (NEW)

> "개념 이해는 시작, 실무 적용이 목표"

모든 학습은 **실무 모범 사례**로 연결되어야 합니다.

---

## 개선된 학습 사이클

### 전체 흐름 (v2.0)

```text
┌─────────────────────────────────────────┐
│ 1. 예측 단계 (학생)                    │
│ - 구체적인 질문 정의                   │
│ - 최소 2개 이상의 가설                 │
│ - 예측한 이유 명시                     │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 2. 사고 체크 (코치)                    │
│ - 모호한 용어 지적                     │
│ - 논리적 모순 찾기                     │
│ - 숨겨진 가정 명시화                   │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 3. 실험 실행 (학생)                    │
│ - HTML 파일로 실행                     │
│ - 결과 정확히 기록                     │
│ - 브라우저 상태 확인                   │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 4. 분석 (학생 → 코치 검증)             │
│ - 예측과 결과 비교                     │
│ - 왜 그런 결과가 나왔는가?            │
│ - 어떤 가정이 틀렸는가?                │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 5. 실무 리팩토링 (NEW)                 │
│ - 실험 코드를 실무 코드로 전환         │
│ - JSDoc 주석 추가                      │
│ - 에러 처리 개선                       │
│ - 테스트 케이스 작성                   │
│ - 코드 리뷰 체크리스트 적용            │
└──────────────┬──────────────────────────┘
               ↓
┌─────────────────────────────────────────┐
│ 6. 새로운 질문 도출                    │
│ - 실험에서 생긴 의문점                 │
│ - 실무에서 발생할 수 있는 문제         │
│ - 다음 실험 아이디어                   │
└─────────────────────────────────────────┘
```

---

## 사고 습관 교정 체크리스트

### 1. 모호한 표현 제거

**❌ 나쁜 습관** → **✅ 좋은 습관**

**일반적인 예:**

| 모호한 표현 | 정확한 표현 |
| --- | --- |
| "나중에 실행된다" | [기술별 정확한 메커니즘 설명] |
| "자동으로 된다" | [구체적인 동작 과정 설명] |
| "빠르다/느리다" | [측정 가능한 지표로 설명] |
| "먼저/나중" | [정확한 실행 순서 명시] |

**기술별 예시:**

**JavaScript (Event Loop):**
| 모호한 표현 | 정확한 표현 |
| --- | --- |
| "나중에 실행된다" | "Call Stack이 비었을 때 Task Queue에서 가져온다" |
| "비동기라서" | "Task Queue에 콜백이 등록된다" |
| "즉시 실행" | "현재 함수가 return한 직후" 또는 "다음 이벤트 루프 턴에" |

**Webpack (번들링):**
| 모호한 표현 | 정확한 표현 |
| --- | --- |
| "자동으로 번들된다" | "entry → loader → plugin 순서로 처리된다" |
| "최적화된다" | "Tree shaking으로 미사용 코드가 제거된다" |
| "빨라진다" | "Code splitting으로 초기 로딩 시간이 X초 감소한다" |

**React (렌더링):**
| 모호한 표현 | 정확한 표현 |
| --- | --- |
| "리렌더된다" | "state 변경으로 컴포넌트 함수가 재실행된다" |
| "최적화된다" | "memo로 props 비교 후 불필요한 렌더 방지" |
| "빠르다" | "Virtual DOM diffing으로 실제 DOM 업데이트 최소화" |

### 2. 가설 제시 습관

**❌ 나쁜 습관**:
"[기술]은 [단일 방식]으로 동작한다" (단일 가설, 대안 없음)

**✅ 좋은 습관**:

- 주 가설: "[예상되는 동작 방식 A]"
- 대안 가설 1: "[가능성 있는 동작 방식 B]"
- 대안 가설 2: "[다른 각도의 동작 방식 C]"

**예시 (JavaScript - setTimeout):**
- 주 가설: "setTimeout(0)은 동기 코드 이후 실행된다"
- 대안 가설 1: "setTimeout(0)은 동기 코드와 동시에 실행된다"
- 대안 가설 2: "setTimeout(0)은 다음 이벤트 루프 턴에 실행된다"

**예시 (Webpack - Tree Shaking):**
- 주 가설: "사용하지 않는 export는 자동으로 제거된다"
- 대안 가설 1: "sideEffects 설정이 필요하다"
- 대안 가설 2: "production 모드에서만 동작한다"

### 3. 예측과 검증

**❌ 나쁜 습관**:

```
// 그냥 실행하고 결과만 확인
[코드 실행]
// 결과: [출력]
// "아, 그렇구나"
```

**✅ 좋은 습관:**

```
// 1. 예측: [예상 결과와 이유]
// 2. 실험: 실행
// 3. 결과: [실제 결과] (예측과 일치/불일치)
// 4. 분석: 왜 이런 결과? [내부 메커니즘 분석]
// 5. 리팩토링: 실무에서는 어떻게 사용?
// 6. 새 질문: [추가로 궁금한 점]
```

**예시 (JavaScript - setTimeout):**

```javascript
// 1. 예측: B가 먼저 출력될 것 (0ms니까)
// 2. 실험: 실행
setTimeout(() => console.log("B"), 0);
console.log("A");
// 3. 결과: A, B (예측과 다름!)
// 4. 분석: 왜 B가 나중? Call Stack과 Task Queue 관계?
// 5. 리팩토링: 실무에서는 어떻게 사용?
// 6. 새 질문: setTimeout은 어디로 가는가?
```

**예시 (Webpack - Dynamic Import):**

```javascript
// 1. 예측: 별도 청크 파일로 분리될 것
// 2. 실험: 빌드
import('./module.js');
// 3. 결과: 0.chunk.js 생성됨 (예측 일치!)
// 4. 분석: 왜 분리? 런타임에 로드되기 때문
// 5. 리팩토링: 로딩 상태 처리, 에러 핸들링
// 6. 새 질문: 청크 파일명은 어떻게 제어?
```

### 4. 논리적 일관성

**❌ 논리적 모순:**

"에러가 발생하지 않을 것"

- "브라우저가 멈춘다"
  = 모순 (멈추는 것도 일종의 에러?)

  **✅ 논리적 일관성:**

"에러가 발생한다"
→ "에러 타입은 RangeError일 것"
→ "에러 발생 시 스크립트 중단"
→ 일관성 있음

---

## 용어 정확성 가이드

### 기술별 핵심 용어

각 기술마다 정확한 용어를 사용하여 사고하고 표현합니다.

#### JavaScript - Call Stack & Event Loop

| 상황      | 정확한 표현                                    |
| --------- | ---------------------------------------------- |
| 함수 호출 | "Call Stack에 PUSH된다" 또는 "쌓인다"          |
| 함수 종료 | "Call Stack에서 POP된다" 또는 "제거된다"       |
| 실행 순서 | "LIFO (Last In First Out)"                     |
| 한계 초과 | "RangeError: Maximum call stack size exceeded" |

| 상황        | ❌ 모호한 표현 | ✅ 정확한 표현                              |
| ----------- | -------------- | ------------------------------------------- |
| setTimeout  | "나중에 실행"  | "Task Queue에 등록, Call Stack이 비면 실행" |
| Promise     | "빠르게 실행"  | "Microtask Queue에 등록"                    |
| async/await | "비동기 처리"  | "함수 실행을 일시 중단 후 재개"             |

#### Webpack - 번들링 프로세스

| 상황 | ❌ 모호한 표현 | ✅ 정확한 표현 |
| --- | --- | --- |
| 번들링 | "자동으로 합쳐진다" | "Entry → Dependency Graph → Loader → Plugin 순서로 처리" |
| Tree Shaking | "안 쓰는 코드 제거" | "ES Module 정적 분석으로 미사용 export 제거" |
| Code Splitting | "파일 분리" | "Dynamic import 지점에서 별도 청크 생성" |

#### React - 렌더링 메커니즘

| 상황 | ❌ 모호한 표현 | ✅ 정확한 표현 |
| --- | --- | --- |
| 렌더링 | "화면이 업데이트된다" | "컴포넌트 함수 재실행 → Virtual DOM diffing → DOM 업데이트" |
| State 변경 | "값이 바뀐다" | "setState 호출 → 리렌더 스케줄링 → 다음 렌더에서 새 값 사용" |
| Effect | "자동으로 실행" | "렌더 완료 후 (DOM 커밋 후) 비동기 실행" |

#### Next.js - 라우팅 & 렌더링

| 상황 | ❌ 모호한 표현 | ✅ 정확한 표현 |
| --- | --- | --- |
| SSR | "서버에서 렌더" | "서버에서 HTML 생성 → 클라이언트에 전송 → Hydration" |
| 페이지 이동 | "페이지 전환" | "Client-side navigation (SPA) 또는 Full refresh" |
| App Router | "자동 라우팅" | "파일 시스템 기반 라우팅 (app/ 디렉토리 구조)" |

### 시점 관련 (범용)

| ❌ 모호함 | ✅ 명확함 |
| --- | --- |
| "즉시" | [기술별 정확한 시점 명시] |
| "나중에" | [기술별 정확한 시점 명시] |
| "자동으로" | [기술별 정확한 메커니즘 설명] |
| "끝나면" | [기술별 정확한 완료 시점 명시] |

---

## 실험 문서 템플릿

모든 실험은 다음 구조를 따릅니다:

# 실험 X.Y: [실험 제목]

## 최상위 질문

[한 문장으로 핵심 질문 정의]

## 실험 환경

**기술 스택:** [사용할 기술/도구]
**실행 방법:** [HTML 파일, Node.js, 번들러 빌드, 브라우저 DevTools 등]
**측정 도구:** [해당되는 경우: Performance API, Webpack Bundle Analyzer, React DevTools 등]

## 실험 코드

```[언어]
[검증할 코드]
```

## 당신의 예측

**[핵심 질문 1]:**

```text
(학생이 작성)
```

**예측한 이유:**

```text
(왜 그렇게 생각하는지, 어떤 가정에 기반하는지)
```

**대안 가설 (최소 1개 이상):**

```text
혹시 다르게 동작할 가능성은?
- 대안 1: ...
- 대안 2: ...
```

---

## 사고 체크 (교정 받을 영역)

**사용한 용어 점검:**

- [ ] "먼저/나중" 같은 모호한 표현 사용했는가?
- [ ] 정확한 시점을 명시했는가?
- [ ] "비동기" 같은 포괄적 용어 대신 구체적으로 설명했는가?

**논리 점검:**

- [ ] 예측과 이유가 논리적으로 일관되는가?
- [ ] 모순되는 진술이 있는가?

**가정 점검:**

- [ ] 숨겨진 가정이 있는가?
- [ ] 그 가정은 검증 가능한가?
- [ ] 이전 실험의 결과를 고려했는가?

---

## 실험 실행

**실제 결과:**

```text
(콘솔 출력을 정확히 기록)
```

**관찰:**

```text
- 예측과 일치하는가?
- 예상과 다른 점은?
- 브라우저 상태는?
- 특이사항은?
```

---

## 분석

**예측과 일치/불일치:**

```text
일치 / 불일치
```

**왜 그런 결과가 나왔는가?**

```text
(원인 분석 - Call Stack, Queue 등 정확한 용어 사용)
```

**틀렸다면, 어떤 가정이 잘못되었나?**

```text
(틀린 경우에만 작성)
- 가정 1이 틀렸음: ...
- 실제로는 ...
```

---

## 실무 리팩토링 (NEW)

### 실험 코드 (개념 이해용)

```javascript
// 실험에서 사용한 간단한 코드
[실험 코드 그대로]
```

### 실무 코드로 전환

```[언어]
/**
 * [함수/모듈 설명]
 *
 * @description
 * [상세 설명]
 *
 * @param {Type} paramName - 파라미터 설명
 * @returns {Type} 반환값 설명
 * @throws {ErrorType} 에러 발생 조건
 *
 * @example
 * // 사용 예시
 * const result = functionName(param);
 */
function/class/module functionName(param) {
  // 1. 입력 검증
  if (!param) {
    throw new [ErrorType]("param is required");
  }

  // 2. 핵심 로직
  // (실험에서 배운 개념 적용)

  // 3. 에러 처리
  try {
    // ...
  } catch (error) {
    // 복구 가능한 에러 처리
    [로깅];
    throw error;
  }

  return result;
}
```

**기술별 예시:**

**JavaScript/TypeScript:**
- JSDoc 또는 TypeScript 타입 주석
- try/catch 에러 처리
- 테스트: Jest, Vitest

**Webpack 설정:**
- 설정 파일에 주석으로 설명
- 에러 처리: webpack.validate
- 테스트: 빌드 결과 검증

**React 컴포넌트:**
- PropTypes 또는 TypeScript
- Error Boundary
- 테스트: React Testing Library

### 개선 포인트

- ✅ 문서화 (주석/타입/설명)
- ✅ 명확한 이름 (함수/변수/설정)
- ✅ 에러 처리 (타입, 범위, 경계 케이스)
- ✅ 매직 값 → 상수화
- ✅ 단일 책임 원칙
- ✅ 테스트 가능한 구조

### 테스트 케이스

```[언어]
describe('[기능명]', () => {
  test('정상 케이스', () => {
    // Given
    const input = ...;

    // When
    const result = functionName(input);

    // Then
    expect(result).toBe(...);
  });

  test('에러 케이스: 잘못된 입력', () => {
    expect(() => functionName(null)).toThrow([ErrorType]);
  });

  test('엣지 케이스: [설명]', () => {
    // 실험에서 발견한 특이 케이스
  });
});
```

### 실무 적용 시나리오

**이 개념이 실무에서 사용되는 경우:**

1. [시나리오 1]

   - 상황: ...
   - 적용 방법: ...
   - 주의점: ...

2. [시나리오 2]

   - 상황: ...
   - 적용 방법: ...
   - 주의점: ...

**안티패턴:**

```[언어]
// ❌ 잘못된 사용
[잘못된 코드/설정 예시]

// ✅ 올바른 사용
[올바른 코드/설정 예시]
```

### 새로운 질문

이 실험에서 생긴 새로운 의문:

```text
질문 1: ...
질문 2: ...
```

실무 관련 질문 (NEW):

```text
실무에서 이 개념이 어떻게 활용되는가?
흔히 발생하는 버그는?
성능에 미치는 영향은?
```

다음 실험 아이디어:

```text
실험 아이디어 1: ...
실험 아이디어 2: ...
```

### 이전 실험과의 연결

실험 X.Y에서 배운 것:

```text
...
```

이번 실험에서 검증할 것:

```text
...
```

---

## 실무 코드 작성 가이드

### 1. 문서화 습관

**기술별 문서화 방법:**

**JavaScript/TypeScript:**

```javascript
/**
 * 함수의 목적을 한 문장으로
 *
 * @description
 * 상세 설명 (선택)
 *
 * @param {Type} paramName - 파라미터 설명
 * @param {Object} options - 옵션 객체
 * @param {number} [options.maxDepth=10] - 최대 깊이 (선택, 기본값 10)
 *
 * @returns {Type} 반환값 설명
 * @throws {ErrorType} 에러 발생 조건
 *
 * @example
 * // 사용 예시
 * const result = myFunction(param, { maxDepth: 20 });
 */
```

**TypeScript:**

```typescript
/**
 * 함수 설명
 */
function myFunction<T>(param: T, options?: Options): Result {
  // TypeScript 타입이 문서 역할도 함
}

interface Options {
  /** 최대 깊이 */
  maxDepth?: number;
}
```

**Webpack 설정:**

```javascript
// webpack.config.js

module.exports = {
  // Entry: 번들링 시작점 정의
  entry: './src/index.js',

  // Output: 번들 결과물 설정
  output: {
    path: path.resolve(__dirname, 'dist'),
    filename: '[name].[contenthash].js', // 캐시 무효화를 위한 contenthash
  },

  // Optimization: Tree shaking, Code splitting 등
  optimization: {
    // 런타임 청크 분리로 캐싱 효율 증대
    runtimeChunk: 'single',
  },
};
```

**React 컴포넌트:**

```typescript
interface ButtonProps {
  /** 버튼 텍스트 */
  children: React.ReactNode;
  /** 클릭 핸들러 */
  onClick: () => void;
  /** 비활성화 여부 */
  disabled?: boolean;
}

/**
 * 재사용 가능한 버튼 컴포넌트
 *
 * @example
 * <Button onClick={handleClick}>클릭</Button>
 */
export function Button({ children, onClick, disabled = false }: ButtonProps) {
  // ...
}
```

### 2. 에러 처리 모범 사례

**기술별 에러 처리:**

**JavaScript/TypeScript - 런타임 에러:**

```javascript
// 1. 커스텀 에러 타입 정의
class ValidationError extends Error {
  constructor(message, field) {
    super(message);
    this.name = "ValidationError";
    this.field = field;
  }
}

// 2. 입력 검증
function processData(data, options = {}) {
  // 필수 파라미터 검증
  if (!data) {
    throw new TypeError("data is required");
  }

  // 타입 검증
  if (typeof data !== "object") {
    throw new TypeError("data must be an object");
  }

  // 범위 검증
  const maxDepth = options.maxDepth ?? 10;
  if (maxDepth < 0 || maxDepth > 100) {
    throw new RangeError("maxDepth must be between 0 and 100");
  }

  // 핵심 로직
  try {
    return doSomething(data, maxDepth);
  } catch (error) {
    // 에러 컨텍스트 추가
    error.message = `Failed to process data: ${error.message}`;
    throw error;
  }
}

// 3. 에러 복구
try {
  const result = processData(data);
} catch (error) {
  if (error instanceof ValidationError) {
    // 복구 가능한 에러 처리
    console.warn("Validation failed, using fallback");
    // 대체 로직
  } else if (error instanceof TypeError) {
    // 프로그래밍 에러 (수정 필요)
    console.error("Invalid input:", error);
    throw error;
  } else {
    // 예상치 못한 에러
    console.error("Unexpected error:", error);
    throw error;
  }
}
```

**Webpack - 빌드 타임 에러:**

```javascript
// webpack.config.js

const webpack = require('webpack');

module.exports = {
  // 설정...

  plugins: [
    // 빌드 시 에러 발생 시 명확한 메시지
    new webpack.ProvidePlugin({
      // 전역 변수 자동 주입 (누락 시 에러)
      process: 'process/browser',
    }),
  ],

  // 에러 발생 시 중단 (CI/CD 환경)
  bail: process.env.NODE_ENV === 'production',

  // 에러 스택 트레이스
  stats: {
    errors: true,
    errorDetails: true,
  },
};
```

**React - 컴포넌트 에러:**

```typescript
// Error Boundary 패턴
class ErrorBoundary extends React.Component<Props, State> {
  state = { hasError: false, error: null };

  static getDerivedStateFromError(error: Error) {
    return { hasError: true, error };
  }

  componentDidCatch(error: Error, errorInfo: ErrorInfo) {
    // 에러 로깅
    console.error('Component error:', error, errorInfo);
  }

  render() {
    if (this.state.hasError) {
      return <ErrorFallback error={this.state.error} />;
    }

    return this.props.children;
  }
}

// 사용
<ErrorBoundary>
  <MyComponent />
</ErrorBoundary>
```

**Next.js - 페이지 에러:**

```typescript
// pages/_error.tsx
function Error({ statusCode }: { statusCode: number }) {
  return (
    <p>
      {statusCode
        ? `An error ${statusCode} occurred on server`
        : 'An error occurred on client'}
    </p>
  );
}

Error.getInitialProps = ({ res, err }) => {
  const statusCode = res ? res.statusCode : err ? err.statusCode : 404;
  return { statusCode };
};

export default Error;
```

### 3. 상수 사용

```javascript
// ❌ 매직 넘버
function recursiveFunction(data, depth) {
  if (depth > 100) throw new Error("Too deep");
  // ...
}

// ✅ 명명된 상수
const MAX_RECURSION_DEPTH = 100;
const DEFAULT_TIMEOUT_MS = 5000;
const RETRY_ATTEMPTS = 3;

function recursiveFunction(data, depth = 0) {
  if (depth > MAX_RECURSION_DEPTH) {
    throw new RecursionError(
      `Maximum recursion depth (${MAX_RECURSION_DEPTH}) exceeded`,
      depth
    );
  }
  // ...
}
```

### 4. 함수 설계 원칙

```javascript
// ❌ 여러 책임
function processUserDataAndSave(userData) {
  // 검증
  if (!userData.email) throw new Error("Invalid");

  // 변환
  const processed = {
    email: userData.email.toLowerCase(),
    name: userData.name.trim(),
  };

  // 저장
  database.save(processed);

  // 알림
  sendEmail(processed.email);
}

// ✅ 단일 책임
function validateUser(userData) {
  if (!userData.email) {
    throw new ValidationError("email is required");
  }
  if (!userData.name) {
    throw new ValidationError("name is required");
  }
}

function normalizeUser(userData) {
  return {
    email: userData.email.toLowerCase().trim(),
    name: userData.name.trim(),
  };
}

function saveUser(userData) {
  validateUser(userData);
  const normalized = normalizeUser(userData);
  return database.save(normalized);
}

// 사용
const user = saveUser(userData);
sendEmail(user.email);
```

### 5. 명명 규칙

```javascript
// ✅ 명확한 이름
const isUserLoggedIn = true; // boolean: is/has/can
const getUserById = (id) => {}; // 함수: 동사로 시작
const MAX_RETRY_COUNT = 3; // 상수: UPPER_SNAKE_CASE

// 배열: 복수형
const users = [];
const errorMessages = [];

// 단수 vs 복수 구분
const user = getUser(); // 하나
const usersList = getUsers(); // 여러 개

// 동사 선택
const fetchData = () => {}; // 네트워크 요청
const getData = () => {}; // 단순 조회
const loadData = () => {}; // 초기화
const updateData = () => {}; // 수정
const removeData = () => {}; // 삭제
```

---

## 코칭 가이드

### Phase 1: 실험 설계

학생의 질문을 받았을 때:

1. 질문을 구체적인 실험으로 변환

   - 추상적 질문: "비동기는 어떻게 동작하나요?"
   - 구체적 실험: "setTimeout(0)과 동기 코드의 실행 순서는?"

2. 실험 코드 제시

   - 변수는 하나만 변경 (다른 조건은 동일)
   - 결과가 명확히 관찰 가능
   - 5-10줄 이내로 간단하게

3. 예측 유도

   - "결과를 예측해보세요"
   - "왜 그렇게 생각하나요?"
   - "다른 가능성은 없나요?"

### Phase 2: 사고 체크

(기존 내용 유지)

### Phase 3: 실험 실행 지원

(기존 내용 유지)

### Phase 4: 분석 검증

(기존 내용 유지)

### Phase 5: 리팩토링 가이드 (NEW)

실험 코드 → 실무 코드 전환 체크리스트:

학생: (실험 완료 후)
"실험은 완료했습니다!"

코치:
"좋습니다! 이제 실무 관점에서 코드를 개선해봅시다.

리팩토링 체크리스트:

□ JSDoc 주석 추가했는가?

- 함수 목적 설명
- 파라미터 타입 및 설명
- 반환값 설명
- 예외 상황 명시

□ 에러 처리가 적절한가?

- 입력 검증
- 커스텀 에러 타입
- 명확한 에러 메시지
- 복구 가능성 고려

□ 매직 넘버/문자열을 상수로 변경했는가?

□ 함수가 단일 책임만 가지는가?

□ 함수/변수명이 명확한가?

- boolean: is/has/can
- 함수: 동사로 시작
- 상수: UPPER_CASE

□ 테스트 케이스를 작성했는가?

- 정상 케이스
- 에러 케이스
- 엣지 케이스 (실험에서 발견한 것)

□ 실무 적용 시나리오를 고려했는가?

- 어떤 상황에서 사용?
- 안티패턴은?
- 성능 고려사항은?"

### Phase 6: 새로운 질문 유도

연결 질문 제시 (기존 내용에 실무 질문 추가):

실험 1.2에서 배운 것:

- Stack Overflow 시 RangeError 발생
- try/catch로 에러를 잡으면 프로그램 계속

개념 연결 질문:
"try/catch가 없으면 어떻게 되나?
→ 실험 1.3: Uncaught Error"

실무 연결 질문 (NEW):
"실무에서 재귀 함수를 안전하게 사용하려면?
→ 미니 프로젝트: Safe Recursion Utility"

---

## 예시 대화 흐름

### 예시 3: 실험 후 리팩토링 (NEW)

학생: "실험 1.2 완료했습니다! Stack Overflow가 발생하네요."

코치:
"훌륭합니다! 이제 배운 개념을 실무에 적용해봅시다.

현재 실험 코드:

```javascript
function recursiveFn() {
  recursiveFn();
}
```

이 코드는 개념 이해용입니다. 실무에서는 어떻게 작성해야 할까요?

과제:

재귀 함수를 안전하게 만들기

- 깊이 제한 추가
- 명확한 에러 메시지
- JSDoc 주석
- 테스트 케이스 작성

정상 재귀
깊이 제한 초과
실제 사용 예시

- 중첩 객체 평탄화
- 파일 트리 탐색

먼저 1번부터 시작해보세요."

학생: (리팩토링 완료)

```javascript
/**
 * 안전한 재귀 함수 (깊이 제한 포함)
 *
 * @param {Function} fn - 재귀 함수
 * @param {number} [maxDepth=100] - 최대 재귀 깊이
 * @returns {Function} 래핑된 함수
 */
function safeRecursive(fn, maxDepth = 100) {
  let depth = 0;

  return function recursive(...args) {
    if (depth >= maxDepth) {
      throw new RecursionError(
        `Maximum recursion depth (${maxDepth}) exceeded`,
        depth
      );
    }

    depth++;
    try {
      return fn.call(this, ...args, recursive);
    } finally {
      depth--;
    }
  };
}
```

코치:
"완벽합니다! 실험에서 배운 Stack Overflow 개념을 실무 코드로 잘 적용했네요.

개선 포인트:
✅ 깊이 제한 (매직 넘버 피함: maxDepth 파라미터)
✅ 커스텀 에러 타입
✅ JSDoc 주석
✅ finally로 depth 정리

다음 단계:

- 테스트 케이스 작성
- 실제 사용 예시 (파일 트리 탐색)
- Anti-pattern 문서화 (무한 재귀 디버깅)"

---

## 학습 진행 체크포인트

### Phase 완료 기준 (v2.0)

각 Phase 완료 시 다음을 확인:

✅ Phase 완료 체크리스트:

### 개념 이해

□ 예측을 정확한 용어로 작성했는가?

- "비동기", "나중에" 같은 모호한 표현 없음
- "Call Stack에 쌓인다", "return 후 제거" 같은 정확한 표현

□ 대안 가설을 제시했는가?

- 주 예측 + 최소 1개 이상의 대안

□ 실험 결과를 정확히 기록했는가?

- 콘솔 출력 정확히 복사
- 브라우저 상태 확인

□ 예측과 결과를 비교 분석했는가?

- 일치/불일치 명시
- 틀린 경우 어떤 가정이 잘못되었는지

### 실무 적용 (NEW)

□ 실험 코드를 실무 코드로 리팩토링했는가?

- JSDoc 주석
- 에러 처리
- 명명 규칙
- 상수 사용

□ 테스트 케이스를 작성했는가?

- 정상 케이스
- 에러 케이스
- 엣지 케이스

□ 실무 적용 시나리오를 정리했는가?

- 사용 케이스
- 안티패턴
- 주의사항

### 다음 단계

□ 새로운 질문을 도출했는가?

- 개념 연결 질문
- 실무 연결 질문

□ 사고 습관이 개선되었는가?

- 모호한 표현 감소
- 논리적 일관성 증가
- 대안 고려 습관화

---

### 문서화 규칙 (v2.0)

모든 학습은 다음과 같이 문서화:

```text
프로젝트 구조:
├── roadmap.md (전체 학습 계획)
├── code-review-checklist.md (코드 리뷰 체크리스트)
├── [기술명]/
│   ├── [주제]/
│   │   ├── README.md (주제 종합 정리)
│   │   ├── experiments/
│   │   │   ├── experiment-X.Y-name.md (실험 문서)
│   │   │   └── experiment-X.Y.[확장자] (실행 파일)
│   │   ├── practices/ (실무 연습)
│   │   │   ├── 01-project-name/
│   │   │   │   ├── README.md
│   │   │   │   ├── src/
│   │   │   │   └── tests/
│   │   │   └── 02-another-project/
│   │   ├── anti-patterns/ (안티패턴)
│   │   │   └── pattern-name.md
│   │   ├── best-practices/ (모범 사례)
│   │   │   └── topic-name.md
│   │   └── docs/ (개념 정리, 추가 자료)
│   │       ├── concept-summary.md
│   │       └── cheatsheet.md
```

**기술별 예시 구조:**

**JavaScript:**
```
javascript/
├── fundamentals/
│   ├── event-loop/
│   │   ├── experiments/
│   │   │   └── experiment-1.1-call-stack.html
```

**Webpack:**
```
bundler/
├── webpack/
│   ├── tree-shaking/
│   │   ├── experiments/
│   │   │   └── experiment-1.1-unused-export/
│   │   │       ├── src/
│   │   │       └── webpack.config.js
```

**React:**
```
react/
├── hooks/
│   ├── useEffect/
│   │   ├── experiments/
│   │   │   └── experiment-1.1-dependency-array.tsx
```

**파일명 규칙**:

- 실험 문서: `experiment-{number}-{주제}.md`
- 실험 파일:
  - JavaScript: `experiment-{number}.html` 또는 `.js`
  - Webpack: `experiment-{number}/` (디렉토리)
  - React: `experiment-{number}.tsx`
- 미니 프로젝트: `01-project-name/`, `02-project-name/`
- 안티패턴: `anti-pattern-{주제}.md` 또는 `{주제}.md`
- 모범 사례: `best-practice-{주제}.md` 또는 `{주제}.md`

---

## 마무리 시 제공할 것

### 1. 커밋 메시지 템플릿 (v2.0)

```text
feat([기술명]/[주제]): [학습 단계] - [제목]

완료한 실험:

실험 X: [제목] (예측 일치 ✅)
실험 Y: [제목] (예측 불일치 → 중요한 발견!)
실험 Z: [제목]

핵심 학습 내용:

[개념 1]
- 세부사항 1
- 세부사항 2

[개념 2]
- 세부사항 1
- 세부사항 2

실무 적용:

- 리팩토링: [실험 코드 → 실무 코드 전환]
- 미니 프로젝트: [프로젝트명]
- 안티패턴 정리: [주제]

학습 방법론:

- 예측 → 실험 → 검증 → 리팩토링 사이클
- 정확한 용어 사용 ([기술별 핵심 용어])
- 실무 모범 사례 적용

파일 구조:

- [기술명]/[주제]/README.md: 주제 종합 정리
- [기술명]/[주제]/experiments/: 실험 문서 및 실행 파일
- [기술명]/[주제]/practices/: 실무 연습 프로젝트
- [기술명]/[주제]/anti-patterns/: 안티패턴 문서

다음 단계: [다음 주제]

🤖 Generated with Claude Code
Co-Authored-By: Claude noreply@anthropic.com
```

**예시 (JavaScript):**
```
feat(javascript/event-loop): Phase 1 - Call Stack

완료한 실험:
실험 1.1: Call Stack의 LIFO 동작 (예측 일치 ✅)
실험 1.2: Stack Overflow 발생 조건 (예측 불일치 → 중요한 발견!)

핵심 학습 내용:
Call Stack:
- LIFO 구조로 함수 실행 관리
- RangeError 발생 시 스크립트 중단
```

**예시 (Webpack):**
```
feat(webpack/tree-shaking): 기본 동작 원리

완료한 실험:
실험 1: 미사용 export 제거 확인
실험 2: sideEffects 설정 영향

핵심 학습 내용:
Tree Shaking:
- ES Module 정적 분석 기반
- production 모드 + optimization.usedExports
```

**예시 (React):**
```
feat(react/hooks): useEffect 의존성 배열

완료한 실험:
실험 1: 의존성 배열 누락 시 동작
실험 2: 빈 배열 vs 생략 비교

핵심 학습 내용:
useEffect:
- 의존성 배열로 실행 시점 제어
- 생략 시 매 렌더마다 실행
```

### 2. 학습 성과 정리 (v2.0)

```markdown
## Phase X 학습 성과

### 체득한 개념

- ✅ 개념 1
- ✅ 개념 2
- ✅ 개념 3

### 사고 습관 개선

- ✅ 모호한 용어 제거
- ✅ 정확한 시점 명시
- ✅ 대안 가설 제시
- ✅ 예측-검증 습관

### 실무 코드 습관 형성 (NEW)

- ✅ JSDoc 주석 작성
- ✅ 에러 처리 개선
- ✅ 테스트 케이스 작성
- ✅ 명명 규칙 적용
- ✅ 리팩토링 습관

### 실험 통계

- 총 실험: X개
- 예측 일치: Y개
- 예측 불일치: Z개 (중요한 발견!)

### 실무 프로젝트

- 완료한 미니 프로젝트: X개
- 정리한 안티패턴: Y개
- 작성한 모범 사례 문서: Z개

### 다음 단계

- Phase Y에서 배울 것
- 연결되는 질문
- 실무 프로젝트 아이디어

이 문서는 실제 학습 과정을 통해 검증되고 개선된 방법론 v2.0입니다.
개념 이해 + 사고 개선 + 실무 코드 습관을 모두 달성할 수 있습니다.
```

---

## 📄 2. 코드 리뷰 체크리스트

**파일 경로**: `javascript/fundamentals/code-review-checklist.md`

````markdown
# 실무 코드 작성 체크리스트

이 체크리스트는 실험 코드를 실무 수준으로 리팩토링할 때 사용합니다.

---

## 전체 체크리스트 (빠른 참조)

□ JSDoc 주석  
□ 에러 처리  
□ 명명 규칙  
□ 상수 사용  
□ 단일 책임  
□ 테스트 작성  
□ 타입 안전성

---

## Level 1: Event Loop 관련

### Call Stack 관련

**재귀 함수 작성 시:**

- [ ] 재귀 깊이 제한이 있는가?

```javascript
// ❌
function recursive(data) {
  return recursive(data.child);
}

// ✅
function recursive(data, depth = 0, maxDepth = 100) {
  if (depth >= maxDepth) {
    throw new RecursionError(`Max depth ${maxDepth} exceeded`);
  }
  return recursive(data.child, depth + 1, maxDepth);
}
```
````

- [ ] Stack Overflow 가능성을 고려했는가?

  - 대안: 반복문 사용
  - 대안: Tail Call Optimization 고려 (제한적)
  - 대안: Trampoline 패턴

- [ ] 에러 발생 시 Call Stack 정리 방법을 고려했는가?

```javascript
// ✅ finally로 정리
function recursiveProcess(data, depth = 0) {
  depth++;
  try {
    // 처리
  } finally {
    depth--; // Stack 정리
  }
}
```

### setTimeout 관련

- [ ] setTimeout(fn, 0)을 동기처럼 사용하지 않았는가?

```javascript
// ❌ 순서 보장 안됨
let result;
setTimeout(() => {
  result = "done";
}, 0);
console.log(result); // undefined

// ✅ Promise 사용
function delay(ms = 0) {
  return new Promise((resolve) => setTimeout(resolve, ms));
}

await delay(0);
console.log(result); // 'done'
```

- [ ] 타이머 콜백에서 this 바인딩을 고려했는가?

```javascript
// ❌
class MyClass {
  constructor() {
    setTimeout(function () {
      console.log(this); // undefined (strict mode)
    }, 0);
  }
}

// ✅ 화살표 함수
class MyClass {
  constructor() {
    setTimeout(() => {
      console.log(this); // MyClass 인스턴스
    }, 0);
  }
}
```

- [ ] 루프 내부 setTimeout의 클로저 문제를 해결했는가?

```javascript
// ❌ var 사용 시 문제
for (var i = 0; i < 5; i++) {
  setTimeout(() => console.log(i), 0);
}
// 출력: 5, 5, 5, 5, 5

// ✅ let 사용
for (let i = 0; i < 5; i++) {
  setTimeout(() => console.log(i), 0);
}
// 출력: 0, 1, 2, 3, 4

// ✅ 클로저 활용
for (var i = 0; i < 5; i++) {
  ((i) => {
    setTimeout(() => console.log(i), 0);
  })(i);
}
```

- [ ] 타이머를 정리(clearTimeout)하는가?

```javascript
// ✅ 컴포넌트 언마운트 시 정리
useEffect(() => {
  const timerId = setTimeout(() => {
    // ...
  }, 1000);

  return () => clearTimeout(timerId);
}, []);
```

### 에러 처리

- [ ] try/catch 위치가 적절한가?

```javascript
// ❌ 너무 넓음
try {
  const data = fetchData();
  processData(data);
  saveData(data);
  sendNotification();
} catch (error) {
  console.error(error);
}

// ✅ 각 단계별 처리
let data;
try {
  data = fetchData();
} catch (error) {
  console.error("Fetch failed:", error);
  return fallbackData;
}

try {
  processData(data);
} catch (error) {
  console.error("Process failed:", error);
  throw new ProcessError("Failed to process", { cause: error });
}
```

- [ ] 에러 메시지가 명확한가?

```javascript
// ❌ 모호함
throw new Error("Invalid input");

// ✅ 명확함
throw new TypeError(
  `Expected 'data' to be an object, got ${typeof data}. ` +
    `Received: ${JSON.stringify(data)}`
);
```

- [ ] 커스텀 에러 타입을 사용하는가?

```javascript
class ValidationError extends Error {
  constructor(message, field) {
    super(message);
    this.name = "ValidationError";
    this.field = field;
  }
}

class RecursionError extends Error {
  constructor(message, depth) {
    super(message);
    this.name = "RecursionError";
    this.depth = depth;
  }
}

// 사용
if (!user.email) {
  throw new ValidationError("Email is required", "email");
}
```

- [ ] 복구 가능한 에러 vs 치명적 에러를 구분하는가?

```javascript
try {
  const result = await fetchData();
} catch (error) {
  if (error instanceof NetworkError) {
    // 복구 가능: 재시도
    return retry(fetchData);
  } else if (error instanceof ValidationError) {
    // 복구 불가: 사용자에게 알림
    showErrorMessage(error.message);
    return null;
  } else {
    // 예상치 못한 에러: 상위로 전파
    throw error;
  }
}
```

---

## 일반 코드 품질

### 1. JSDoc 주석

- [ ] 모든 public 함수에 JSDoc이 있는가?

```javascript
/**
 * 사용자를 ID로 조회
 *
 * @param {string} userId - 사용자 ID
 * @param {Object} [options] - 옵션
 * @param {boolean} [options.includeDeleted=false] - 삭제된 사용자 포함 여부
 * @returns {Promise<User>} 사용자 객체
 * @throws {NotFoundError} 사용자를 찾을 수 없을 때
 * @throws {DatabaseError} DB 오류 발생 시
 *
 * @example
 * const user = await getUserById('123');
 * const user = await getUserById('123', { includeDeleted: true });
 */
async function getUserById(userId, options = {}) {
  // ...
}
```

- [ ] 복잡한 로직에 설명 주석이 있는가?

```javascript
// ✅ "왜"를 설명하는 주석
// Event Loop가 다음 턴에 실행되도록 보장하기 위해 setTimeout 사용
// Promise.resolve()는 Microtask Queue에 들어가므로 부적절
setTimeout(() => {
  updateUI();
}, 0);
```

### 2. 명명 규칙

- [ ] 함수명이 동작을 명확히 설명하는가?

```javascript
// ❌ 모호함
function handle(data) {}
function process(item) {}
function doIt() {}

// ✅ 명확함
function validateUserInput(data) {}
function transformDataToDTO(item) {}
function fetchUserProfile() {}
```

- [ ] Boolean 변수는 is/has/can으로 시작하는가?

```javascript
// ❌
let login = true;
let data = false;

// ✅
let isLoggedIn = true;
let hasData = false;
let canEdit = true;
```

- [ ] 상수는 UPPER_SNAKE_CASE를 사용하는가?

```javascript
// ❌
const maxRetry = 3;
const defaultTimeout = 5000;

// ✅
const MAX_RETRY_COUNT = 3;
const DEFAULT_TIMEOUT_MS = 5000;
const API_BASE_URL = "https://api.example.com";
```

- [ ] 배열/컬렉션은 복수형을 사용하는가?

```javascript
// ❌
const user = getUsers();
const item = fetchItems();

// ✅
const users = getUsers();
const items = fetchItems();

// 단수 vs 복수 명확히
const user = users[0];
const firstItem = items.find((item) => item.id === 1);
```

### 3. 매직 넘버/문자열

- [ ] 숫자 리터럴을 의미 있는 상수로 변경했는가?

```javascript
// ❌
setTimeout(callback, 5000);
if (depth > 100) throw new Error("Too deep");

// ✅
const DEBOUNCE_DELAY_MS = 5000;
const MAX_RECURSION_DEPTH = 100;

setTimeout(callback, DEBOUNCE_DELAY_MS);
if (depth > MAX_RECURSION_DEPTH) {
  throw new RecursionError("Too deep", depth);
}
```

- [ ] 문자열 리터럴을 상수로 추출했는가?

```javascript
// ❌
if (user.role === "admin") {
}
if (status === "pending") {
}

// ✅
const USER_ROLES = {
  ADMIN: "admin",
  USER: "user",
  GUEST: "guest",
};

const ORDER_STATUS = {
  PENDING: "pending",
  COMPLETED: "completed",
  CANCELLED: "cancelled",
};

if (user.role === USER_ROLES.ADMIN) {
}
if (status === ORDER_STATUS.PENDING) {
}
```

### 4. 함수 설계

- [ ] 함수가 하나의 책임만 가지는가? (SRP)

```javascript
// ❌ 여러 책임
function saveUser(userData) {
  // 검증
  if (!userData.email) throw new Error("Invalid");

  // 변환
  const normalized = normalizeEmail(userData.email);

  // 저장
  db.save({ ...userData, email: normalized });

  // 알림
  sendEmail(normalized);
}

// ✅ 단일 책임
function validateUser(userData) {
  if (!userData.email) {
    throw new ValidationError("email is required");
  }
}

function normalizeUser(userData) {
  return {
    ...userData,
    email: normalizeEmail(userData.email),
  };
}

function saveUser(userData) {
  return db.save(userData);
}

// 조합
async function createUser(userData) {
  validateUser(userData);
  const normalized = normalizeUser(userData);
  const user = await saveUser(normalized);
  await sendWelcomeEmail(user.email);
  return user;
}
```

- [ ] 파라미터 개수가 적절한가? (3개 이하 권장)

```javascript
// ❌ 파라미터 많음
function createUser(name, email, age, city, country, phone, role) {}

// ✅ 객체 사용
function createUser({ name, email, age, address, phone, role }) {
  const { city, country } = address;
  // ...
}
```

- [ ] 부수 효과(side effect)가 명확히 드러나는가?

```javascript
// ❌ 숨겨진 부수 효과
function getUser(id) {
  analytics.track("user_viewed"); // 예상 못한 부수 효과
  return db.findById(id);
}

// ✅ 명확한 이름
function getUserAndTrack(id) {
  analytics.track("user_viewed");
  return db.findById(id);
}

// 더 나은 방법: 분리
function getUser(id) {
  return db.findById(id);
}

const user = getUser(id);
analytics.track("user_viewed");
```

### 5. 테스트 가능성

- [ ] 함수가 테스트하기 쉬운 구조인가?

```javascript
// ❌ 테스트 어려움 (전역 상태 의존)
function processData() {
  const data = window.globalData;
  const result = transform(data);
  window.result = result;
}

// ✅ 테스트 쉬움 (순수 함수)
function processData(data) {
  return transform(data);
}
```

- [ ] 의존성 주입을 사용하는가?

```javascript
// ❌ 하드코딩된 의존성
function fetchUserData(userId) {
  return axios.get(`/api/users/${userId}`);
}

// ✅ 의존성 주입
function fetchUserData(userId, httpClient = axios) {
  return httpClient.get(`/api/users/${userId}`);
}

// 테스트 시
const mockClient = { get: jest.fn() };
fetchUserData("123", mockClient);
```

---

## 레벨별 추가 체크리스트

### Level 0: 코드 전달 메커니즘

- async/defer 선택 이유가 명확한가?

### Level 1-A: Event Loop

- 이 문서에서 다룬 내용 참조

### Level 1-B: DOM 이벤트 (예정)

- (Level 1-B 학습 시 추가)

---

## 코드 리뷰 프로세스

### 1. 자가 리뷰 (Self Review)

실험 코드 작성 후:

- □ 위 체크리스트 항목 확인
- □ 개선 포인트 3가지 선정
- □ 리팩토링 실행
- □ 변경 전/후 비교

### 2. 페어 리뷰 (Optional)

동료나 멘토에게:

- □ 개념 설명
- □ 리팩토링 이유 설명
- □ 피드백 수렴
- □ 추가 개선

### 3. 문서화

- □ 실험 문서의 "리팩토링" 섹션 작성
- □ Before/After 코드 비교
- □ 개선 포인트 나열
- □ 배운 점 정리

---

## 참고 자료

### 코드 스타일 가이드

- Airbnb JavaScript Style Guide
- Google JavaScript Style Guide
- Clean Code JavaScript

### 에러 처리

- Error Handling Best Practices

### JSDoc

- JSDoc Official Documentation
- TypeScript JSDoc Reference

이 체크리스트는 학습이 진행됨에 따라 계속 업데이트됩니다.
